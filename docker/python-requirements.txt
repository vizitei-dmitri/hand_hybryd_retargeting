# Pinned because dex-retargeting 0.5.0 was released and tested against the
# Pinocchio 3.x API. Newer pin 4.x releases are not assumed compatible here.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy==2.2.6
pin==3.4.0
# Pinocchio 3.4 is linked against urdfdom 4.x. The unbounded transitive
# dependency otherwise resolves to urdfdom 6.x and fails at import time.
cmeel-urdfdom==4.0.1
cmeel-tinyxml2==10.0.0
nlopt==2.9.1
# A coherent CPU-only set supported by LeRobot 0.4.4. CUDA is not required for
# the bridge or retargeting and would make the image hardware-specific.
torch==2.10.0+cpu
torchvision==0.25.0+cpu
torchcodec==0.10.0
dex-retargeting==0.5.0
mujoco==3.3.7
lerobot==0.4.4
